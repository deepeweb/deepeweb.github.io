function toggleMenu(){const menu=document.getElementById('menu');const hamburger=document.querySelector('.hamburger-icon');menu.classList.toggle('active');if(menu.classList.contains('active')){hamburger.innerHTML='&#10006;'}else{hamburger.innerHTML='&#9776;'}}
document.addEventListener("DOMContentLoaded",async function(){let pages=[];let extraPages=["../all_notes.html"];for(let page of extraPages){await fetch(page).then(response=>response.text()).then(html=>{let tempDiv=document.createElement("div");tempDiv.innerHTML=html;tempDiv.querySelectorAll("a").forEach(link=>{let url=link.getAttribute("href");let title=link.innerText||url;if(url&&!url.startsWith("http")){pages.push({title,url})}})}).catch(error=>console.error(`Error loading ${page}:`,error))}
window.searchNotes=function(){let query=document.getElementById('searchBox').value.toLowerCase();let resultsDiv=document.getElementById('searchResults');resultsDiv.innerHTML="";if(query==="")return;let filteredResults=pages.filter(page=>page.title.toLowerCase().includes(query)||page.url.toLowerCase().includes(query));if(filteredResults.length===0){resultsDiv.innerHTML="<p style='color: black;'>No results found</p>";return}
filteredResults.forEach(page=>{let resultItem=document.createElement("div");resultItem.innerHTML=`<a href="${page.url}">${page.title}</a>`;resultsDiv.appendChild(resultItem)})};window.handleInputChange=function(){const searchBox=document.getElementById('searchBox');const clearButton=document.getElementById('clearButton');if(searchBox.value.trim()!==''){clearButton.style.display='block'}else{clearButton.style.display='none'}};window.clearSearch=function(){const searchBox=document.getElementById('searchBox');const clearButton=document.getElementById('clearButton');searchBox.value='';clearButton.style.display='none';document.getElementById('searchResults').innerHTML=''}});document.addEventListener("DOMContentLoaded",function(){document.querySelectorAll('a[href^="#"]').forEach(anchor=>{anchor.addEventListener("click",function(e){e.preventDefault();const targetId=this.getAttribute("href").substring(1);const targetElement=document.getElementById(targetId);if(targetElement){targetElement.scrollIntoView({behavior:"smooth"});if(history.pushState){history.pushState(null,null,window.location.pathname)}}})})});const searchBox=document.getElementById('searchBox');const searchResults=document.getElementById('searchResults');function toggleResults(){if(searchBox.value.trim()!==""){searchResults.style.display="block"}else{searchResults.style.display="none"}}
searchBox.addEventListener('input',toggleResults);searchBox.addEventListener('change',toggleResults);searchBox.addEventListener('keyup',toggleResults);setInterval(toggleResults,200);function closeAd(){document.getElementById("footerAd").style.display="none"}
fetch('../dynamic-containers.html').then(response=>response.text()).then(data=>{document.getElementById('dynamic-container').innerHTML=data}).catch(error=>console.error('Error loading container:',error));fetch('side-nav-iwt.html').then(response=>response.text()).then(data=>{document.getElementById('side-nav-iwt').innerHTML=data}).catch(error=>console.error('Error loading container:',error));fetch('side-nav-blog.html').then(response=>response.text()).then(data=>{document.getElementById('side-nav-blog').innerHTML=data}).catch(error=>console.error('Error loading container:',error))
